dovecot_config_version = 2.4.0
dovecot_storage_version = 2.4.0

###############
# General
###############
log_path = /dev/stderr
auth_verbose=yes
mail_debug=yes
login_log_format_elements = user=<%{user}> method=%{mechanism} rip=%{remote_ip} rport=%{remote_port} lip=%{local_ip} lport=%{local_port} mpid=%{mail_pid} %{secured}
protocols {
  sieve = yes
  imap = yes
  pop3 = yes
  lmtp = yes
  submission = yes
}
postmaster_address = {{ POSTMASTER }}@{{ DOMAIN }}
hostname = {{ HOSTNAMES.split(",")[0] }}
mail_utf8_extensions = yes
submission_host = {{ SMTP_ADDRESS }}
submission_relay_host = {{ SMTP_ADDRESS }}
submission_relay_port = 10025
submission_relay_trusted = yes
submission_relay_ssl = no
submission_max_mail_size = {{ MESSAGE_SIZE_LIMIT }}
submission_backend_capabilities = 8BITMIME CHUNKING DSN ENHANCEDSTATUSCODES PIPELINING VRFY
# Advertise only explicitly configured backend capabilities. In particular,
# BURL is not supported by the Postfix proxy target.
submission_client_workarounds = exotic-backend mailbox-for-path whitespace-before-path
imap_urlauth_host =
lmtp_proxy = yes
lmtp_client_workarounds = whitespace-before-path mailbox-for-path

{%- if SUBNET6 %}
listen = *,::
{% else %}
listen = *
{% endif %}

default_internal_user = dovecot
default_login_user = mail
default_internal_group = dovecot

haproxy_trusted_networks = {% if REAL_IP_FROM %}{% for from_ip in REAL_IP_FROM.split(',') %}{{ from_ip }} {% endfor %}{% endif %}

###############
# Authentication
###############
auth_username_chars =
auth_mechanisms = plain login

{%- if TLS %}
ssl = required
ssl_server_cert_file = {{ TLS[0] }}
ssl_server_key_file = {{ TLS[1] }}
{%- if TLS_FLAVOR in ['letsencrypt','mail-letsencrypt'] %}
ssl_server_alt_cert_file = {{ TLS[2] }}
ssl_server_alt_key_file = {{ TLS[3] }}
{% endif %}
# intermediate configuration
ssl_min_protocol = TLSv1.2
ssl_cipher_list = ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305
ssl_server_prefer_ciphers = client
ssl_options = no_ticket
{% else %}
auth_allow_cleartext = yes
ssl = no
{% endif %}

# use_worker defaults to no; leaving it out would move the lookup out of the
# auth-worker process into the auth process.
passdb lua {
  lua_file = /etc/dovecot/login.lua
  use_worker = yes
}

service auth-worker {
  user = dovenull
  group = dovenull
  unix_listener auth-worker {
  }
}

service anvil {
  unix_listener anvil-auth-penalty {
    mode = 0
  }
}

{%- if PORT_4190 %}
service managesieve-login {
  executable = managesieve-login
  inet_listener sieve {
    port = 4190
{%- if PROXY_PROTOCOL_4190 %}
    haproxy = yes
{% endif %}
  }
  inet_listener sieve-webmail {
    port = 14190
  }
  restart_request_count = unlimited
  client_limit = 25000
  process_min_avail = {{ CPU_COUNT }}
  process_limit = {{ CPU_COUNT }}
  vsz_limit = 256M
}
{% endif %}

protocol imap {
  mail_max_userip_connections = 20
  imap_idle_notify_interval = 29mins
}

service imap-login {
  inet_listener imap {
    port = {% if PORT_143 %}143{% else %}0{% endif %}
{%- if PROXY_PROTOCOL_143 %}
    haproxy = yes
{% endif %}
  }
{%- if TLS_993 %}
  inet_listener imaps {
    port = 993
    ssl = yes
{%- if PROXY_PROTOCOL_993 %}
    haproxy = yes
{% endif %}
  }
{% endif %}
  inet_listener imap-webmail {
   port = 10143
  }
  restart_request_count = unlimited
  client_limit = 25000
  process_min_avail = {{ CPU_COUNT }}
  process_limit = {{ CPU_COUNT }}
  vsz_limit = 256M
}

service pop3-login {
  inet_listener pop3 {
    port = {% if PORT_110 %}110{% else %}0{% endif %}
{%- if PROXY_PROTOCOL_110 %}
    haproxy = yes
{% endif %}
  }
{%- if TLS_995 and PORT_995 %}
  inet_listener pop3s {
    port = 995
    ssl = yes
{%- if PROXY_PROTOCOL_995 %}
    haproxy = yes
{% endif %}
  }
{% endif %}
  restart_request_count = unlimited
  client_limit = 25000
  process_min_avail = {{ CPU_COUNT }}
  process_limit = {{ CPU_COUNT }}
  vsz_limit = 256M
}

recipient_delimiter = {{ RECIPIENT_DELIMITER }}
service lmtp {
  user = dovecot
  inet_listener lmtp {
    port = 2525
  }
}

service submission-login {
   inet_listener submission {
    port = {% if PORT_587 %}587{% else %}0{% endif %}
{%- if PROXY_PROTOCOL_587 %}
    haproxy = yes
{% endif %}
   }
{%- if TLS_465 %}
   inet_listener submissions {
    port = 465
    ssl = yes
{%- if PROXY_PROTOCOL_465 %}
    haproxy = yes
{% endif %}
   }
{% endif %}
   inet_listener submission-webmail {
    port = 10025
   }
  restart_request_count = unlimited
  client_limit = 25000
  process_min_avail = {{ CPU_COUNT }}
  process_limit = {{ CPU_COUNT }}
  vsz_limit = 256M
}

!include_try /overrides/dovecot/proxy.conf
