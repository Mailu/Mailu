{% if ANTIVIRUS == 'clamav' %}
clamav {
  scan_mime_parts = true;
  symbol = "CLAM_VIRUS";
  type = "clamav";
  servers = "{{ ANTIVIRUS_ADDRESS }}:3310";
<<<<<<< HEAD
  timeout = 60;
=======
  timeout = 5;
>>>>>>> 086c05a4 (Make rspamd retry for longer when connecting to clamav)
  retransmits = 10;
  {% if ANTIVIRUS_ACTION|default('discard') == 'reject' %}
  action = "reject"
  {% endif %}
}
{% endif %}
.include(try=true,priority=1,duplicate=merge) "/overrides/antivirus.conf"
